import CoreGraphics

enum UIConstants {
    enum Button {
        static let defaultMinHeight: CGFloat = 46
        static let compactMinSize: CGFloat = 42
        static let horizontalPadding: CGFloat = 20
        static let verticalPadding: CGFloat = 10
    }

    enum Radius {
        static let buttonCorner: CGFloat = 14
    }
}
